"""scripts package marker.

This file exists so the build harness and TUI can use absolute imports of
the form `from scripts.build_harness.harness.config import ...` without
relying on `sys.path` manipulation tricks.
"""
